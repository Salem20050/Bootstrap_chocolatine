name: ripo
run-name: Testing my first GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.1.1
                
      - run: echo "Bootstrap_chocolatine"
      - run: du -skh --exclude=".git"
